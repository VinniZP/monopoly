import { DynamicTileDirective } from './dynamic-tile.directive';

describe('DynamicTileDirective', () => {
  it('should create an instance', () => {
    const directive = new DynamicTileDirective();
    expect(directive).toBeTruthy();
  });
});
