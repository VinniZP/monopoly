import { PlayerPositionDirective } from './player-position.directive';

describe('PlayerPositionDirective', () => {
  it('should create an instance', () => {
    const directive = new PlayerPositionDirective();
    expect(directive).toBeTruthy();
  });
});
